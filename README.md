# POC-Iphone-Android

Android camera proof of concept: Kotlin, Jetpack Compose, CameraX.

Photo capture with multi-frame processing, video recording with a cinematic mode.

## Build

```
./gradlew assembleDebug
```

Requires JDK 21 and the Android SDK (compileSdk 36).
